# Dotfiles

Personal config for macOS (Apple Silicon).

## Installation

1. Prepare local files:
   - config/mise/config.toml (mise config)
   - config/git/config.local (git auth info)

2. Run script:

```bash
./scripts/install.sh
```

Note: Raycast settings must be configured manually.
